//! Build script: generates tonic/prost code from `proto/synapse.proto`.
//!
//! Full codegen is wired up in TASK-002; this stub ensures the build succeeds now.

fn main() -> Result<(), Box<dyn std::error::Error>> {
    // Codegen will be enabled in TASK-002.
    Ok(())
}
