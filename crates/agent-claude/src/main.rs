//! Claude Code agent adapter binary.
//!
//! Implements the [`CodingAgent`](shared_types::CodingAgent) trait for the
//! Claude Code CLI and runs the agent poll loop.

fn main() {
    // Stub — full implementation in TASK-018.
}
